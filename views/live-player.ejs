<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Live Video</title>
    <link rel="stylesheet" href="/stylesheets/live-player.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
</head>
<body>
    <section>
        <div class="container">
            <div class="player">
                <div class="local-video">
                    <video id="debater1Video" autoplay playsinline></video>
                </div>
                <div class="remote-video">
                    <video id="debater2Video" autoplay playsinline></video>
                </div>
            </div>
            <div class="about-video">
                <p class="title"><%= Live.title %></p>
                <div class="description">
                    <p><%= Live.description %></p>
                </div>
            </div>
            <div class="video-user-info">
                <img src="/images/Default.png" alt="Default Avatar" class="user-avatar">
                <div class="creator-details">
                    <a href="/creator/creator" class="creator-name">
                        Creator Username
                    </a>
                    <p class="followers-count"><%= follower %> Followers</p>
                </div>
                <button class="follow-btn" data-creator-id="<%= Live.creator[0]._id %>">
                    <% if (isFollowing) { %>
                        <span class="unfollow">Unfollow</span>
                    <% } else { %>
                        <span class="follow">Follow</span>
                    <% } %>
                </button>
            </div> 
        </div>
        <label class="comment-btn" for="commentsToggle">
            <i class="fas fa-comment"></i> 
        </label>
        <input type="checkbox" id="commentsToggle" class="comments-toggle" hidden>
        <div class="comments">
            <div class="comment-list">
                <ul id="commentList">
                    <li class="single-comment">
                        <strong>Krishna tera Shaka : </strong>
                        <p>Me hu Daro Mat Sachin, Tum Bus Mehnat Karte Raho aur mere Priyata ka nam lete Raho, Aur Jo Tum Kar Rahe Ho Wo Karte Raho</p>
                    </li>
                </ul>
            </div>
            <div class="comment-input">
                <input type="text" placeholder="Add a comment" id="commentInput">
                <button id="commentButton">Comment</button>
            </div>
        </div>
    </section>

    <script src="/javascript/socket.io.js"></script>
    <script>
        const socket = io(); // Connect to the signaling server
        const debater1Video = document.getElementById('debater1Video');
        const debater2Video = document.getElementById('debater2Video');

        // Assume that the server will handle the signaling and provide video streams for both debaters.
        
        // Handle incoming streams for both debaters
        socket.on('stream', (data) => {
            if (data.debaterId === 'debater1') {
                debater1Video.srcObject = data.stream; // Assign stream for debater 1
            } else if (data.debaterId === 'debater2') {
                debater2Video.srcObject = data.stream; // Assign stream for debater 2
            }
        });

        // Handle comments
        document.getElementById('commentButton').addEventListener('click', () => {
            const commentInput = document.getElementById('commentInput');
            const comment = commentInput.value.trim();
            if (comment) {
                socket.emit('comment', { user: 'Viewer', text: comment });
                commentInput.value = ''; // Clear input
            }
        });

        // Listen for comments from the server
        socket.on('comment', (data) => {
            const commentList = document.getElementById('commentList');
            const commentItem = document.createElement('li');
            commentItem.className = 'single-comment';
            commentItem.innerHTML = `<strong>${data.user}:</strong> <p>${data.text}</p>`;
            commentList.appendChild(commentItem);
        });
    </script>
</body>
</html>