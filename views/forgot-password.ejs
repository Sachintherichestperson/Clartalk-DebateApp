<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #000000;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        form {
            display: flex;
            flex-direction: column;
            align-items: center;
            background-color: #1a1a1a;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
            position: relative;
            width: 40%;
            top: 100px;
            border: 1px solid #e68a00;
        }
        input[type="email"] {
            padding: 10px;
            margin: 10px 0;
            border: none;
            border-radius: 5px;
            width: 100%;
            max-width: 500px;
            background-color: #4f4e4e;
            color: white;
            font-size: 18px;
        }
        button{
            padding: 10px 20px;
            background-color: #e68a00;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
        }
        .flash-message {
    background-color: #ff4d4d;
    color: white;
    padding: 10px;
    margin-top: 10px;
    text-align: center;
    border-radius: 4px;
    animation: fadeIn 0.3s ease-in-out;
  }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(-10px); }
    to { opacity: 1; transform: translateY(0); }
  }
    </style>
</head>
<body>
    <form action="/login-otp" method="POST">
        <% if (err && err.length > 0) { %>
            <div id="flashMsg" class="err flash-message"><%= err %></div>
        <% } %>   
        <input type="email" name="email" placeholder="Enter your email" required>
        <button type="submit">Send OTP</button>
    </form>
    <script>
        // Flash message disappears after 5 seconds
        const flashMsg = document.getElementById('flashMsg');
        if (flashMsg) {
          setTimeout(() => {
            flashMsg.style.display = 'none';
          }, 5000);
        }
      </script>
</body>
</html>