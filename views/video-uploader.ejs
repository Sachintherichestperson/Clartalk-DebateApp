<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Video | ClarTalk</title>
    <style>
        /* 🌙 Dark theme styling */
        body {
            background-color: #121212;
            color: #ffffff;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .form-container {
            background: #1e1e1e;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(255, 255, 255, 0.1);
            text-align: center;
            width: 400px;
        }

        h4 {
            color: #ffcc00;
            margin-bottom: 15px;
        }

        .input-group {
            margin-bottom: 15px;
        }

        .form-input {
            width: 100%;
            padding: 10px;
            border: 2px solid #ffcc00;
            border-radius: 5px;
            background: #282828;
            color: white;
        }

        button {
            background: #ffcc00;
            color: #121212;
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            width: 100%;
            transition: 0.3s;
        }

        button:hover {
            background: #e6b800;
        }

        /* 🚀 Message Styling */
        .upload-message {
            display: none;
            margin-top: 10px;
            color: #ffcc00;
            font-size: 14px;
        }
    </style>
</head>
<body>

    <div class="form-container">
        <form action="/creator/entertainment/uploaded" method="post" enctype="multipart/form-data" id="upload-form">
            <div class="input-group">
                <h4 class="form-label">Upload Video</h4>
                <input type="file" name="vedio" class="form-input" required>
            </div>
            <button type="submit" id="submit-button">Submit</button>
        </form>
        <p class="upload-message" id="upload-message">⏳ Uploading... This may take 30 sec to 1 min. Do not close the window.</p>
    </div>

    <script>
        document.getElementById("upload-form").addEventListener("submit", function() {
            document.getElementById("upload-message").style.display = "block";
        });
    </script>

</body>
</html>
